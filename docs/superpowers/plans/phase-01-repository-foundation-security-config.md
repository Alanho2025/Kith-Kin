# Phase 01: Repository Foundation and Security Configuration Implementation Plan

Status: implemented; checkpoint results are recorded in `docs/CODE_PLAN.md`

> **For agentic workers:** strict RED-GREEN-REFACTOR. Configuration is tested before creation.

## Goal

Create the Python/React monorepo foundation, safe environment templates, ignore rules, deterministic dependency configuration, Docker PostgreSQL services, and baseline quality commands.

## Non-Goals

No business endpoints, Live connection, database models, ADK logic, MCP server, or UI workflow.

## Source of Truth

`docs/CODE_PLAN.md`, Phase 00 artifacts, `AGENTS.md`, clean-code rules.

## Previous Phase Artifacts

Canonical names and the recorded Live validation topology.

## Entry Conditions

Phase 00 contract scan is clean. Python 3.11 is available through `uv`.

## Exit Checkpoint

Fresh dependency installs succeed; hygiene/config tests, lint, and typecheck pass without credentials.

## Files

### Create

- `.gitignore`, `.env.example`, `docker-compose.yml`, `.python-version`
- `backend/.env.example`, `backend/pyproject.toml`, `backend/requirements.txt`, `backend/app/__init__.py`, `backend/app/core/config.py`
- `backend/tests/unit/core/test_config.py`, `backend/tests/unit/test_repo_hygiene.py`
- `frontend/.env.example`, `frontend/package.json`, `frontend/package-lock.json`, TypeScript/Vite/Vitest/ESLint/Tailwind configs
- `frontend/src/app/App.tsx`, `frontend/src/test/setup.ts`, `frontend/src/app/App.test.tsx`

### Modify

- `AGENTS.md`, `README.md`

### Test

- Backend settings and repo hygiene tests
- Frontend initial render test

### Fixtures

None.

### Migrations

None.

## Public Types

`Settings` contains exactly the environment fields defined in `docs/CODE_PLAN.md`. It validates production secret/cookie policy, ticket TTL 1–300 seconds, positive RAG limits, and allowed origins.

## Error Codes

- `CONFIG_SECRET_REQUIRED`
- `CONFIG_INSECURE_COOKIE`
- `CONFIG_TOKEN_TTL_INVALID`
- `CONFIG_RAG_LIMIT_INVALID`
- `CONFIG_ORIGIN_REQUIRED`

## Task 1: Repo hygiene

### RED

Write `test_repo_hygiene.py` to assert required example files and ignore patterns exist, frontend env has no forbidden key names, and no tracked secret file pattern is present.

### Verify RED

```bash
python3 -m pytest backend/tests/unit/test_repo_hygiene.py -v
```

Expected: failed assertions naming missing `.gitignore` and env example files.

### GREEN

Create the exact env and ignore files from the master plan.

### Verify GREEN

Focused hygiene test passes.

### REFACTOR

Move repeated path/key sets into test constants only; rerun.

## Task 2: Typed settings

### RED

Write complete tests for safe development defaults and each invalid production configuration.

### Verify RED

Expected import failure for missing `app.core.config.Settings`.

### GREEN

Implement `pydantic-settings` models and validators with no import-time environment mutation.

### Verify GREEN

```bash
uv run --project backend pytest backend/tests/unit/core/test_config.py -v
```

### REFACTOR

Extract ticket and RAG nested settings only if the public field names remain unchanged.

## Task 3: Tooling scaffold

Create package manifests and minimal application/test entrypoints. `pyproject.toml` is authoritative; `requirements.txt` is generated by `uv export`. Commit lockfiles.

## Checkpoint Commands

```bash
docker compose config
uv sync --project backend --all-groups
uv run --project backend pytest backend/tests/unit -v
uv run --project backend ruff check backend
uv run --project backend mypy backend/app
npm --prefix frontend ci
npm --prefix frontend run lint
npm --prefix frontend run typecheck
npm --prefix frontend run test -- --run
```

## Rollback

Remove only created scaffold files and Docker containers; no volume may contain user data.

## Stop Conditions

Dependency resolution, hygiene, lint, typecheck, or tests failing blocks Phase 02.

## Commit Boundaries

- `chore(repo): add safe environment and ignore policy`
- `chore(tooling): scaffold backend and frontend quality gates`

## Artifacts Exposed to Next Phase

Settings API, package manifests, test runners, stable source roots, and safe local configuration.

## Implementation Record

- Exact manifests: `backend/pyproject.toml`, `backend/uv.lock`, generated `backend/requirements.txt`, `frontend/package.json`, `frontend/package-lock.json`.
- Safe configuration: root/backend/frontend environment examples; no Gemini ephemeral settings in the MVP.
- Hygiene controls: `.gitignore`, `test_repo_hygiene.py`, `scripts/check_no_secrets.py`, and bundle-scan regression test.
- Toolchain: Python 3.11 pin with runtime support `<3.13`; npm dependencies are exact versions and installed through the lockfile.
- PostgreSQL: `docker-compose.yml` defines isolated dev/test services; Phase 01 creates no schema or data.
- RED evidence: missing config/domain/frontend imports and missing secret-scan command were observed before GREEN.
- Rollback: remove scaffold/config files only; no database volume is mutated by this phase.
